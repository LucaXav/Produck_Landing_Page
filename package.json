{
  "name": "produck-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Produck landing — fixed-fold, duck waddles + draws.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1"
  }
}
